#' Downstream Distances
#'
#' Calculates the distance downstream of each cross section in a channel.
#'
#' @param channel A channel object.
#' @returns A numeric vector of distances downstream for each cross section
#' in `channel`.
#' @note This is a useful function for sorting cross sections in a channel
#' if they are not already sorted.
#' @examples
#' xt_distance_downstream(demo_channel)
#' @export
xt_distance_downstream <- function(channel) {
  stop("TO DO")
}
