% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xt_channel.R
\name{xt_channel}
\alias{xt_channel}
\title{Construct a channel object (\code{xchan})}
\usage{
xt_channel(.plan, .profile = NULL, .axis = NULL, .chainage = NULL, ...)
}
\arguments{
\item{.plan}{Planimetric cross sections as \code{sfc_LINESTRING}.}

\item{.profile}{Optional list of \code{xs_profile} objects, same length as
\code{.plan}.}

\item{.axis}{Optional channel axis as a single \code{LINESTRING} (\code{sfc}/\code{sfg});
same CRS as \code{.plan}. Used by \code{\link[=xt_trace_centerline]{xt_trace_centerline()}} and related functions.}

\item{.chainage}{Optional numeric vector (length \code{length(.plan)}): distance
along the channel from upstream to each section’s station (CRS units); see
\code{\link[=xt_generate_plan]{xt_generate_plan()}}.}

\item{...}{Additional columns for the channel table (vectors or
list-columns).}
}
\value{
An object of class \code{"xchan"}.
}
\description{
Assembles a channel table from \strong{required} planimetric cross sections
(\code{sfc_LINESTRING}), optional profile cross sections (\code{xs_profile} list), and
optional extra columns via \code{...}, in the same spirit as \code{\link[sf:sf]{sf::st_sf()}}. To
\strong{coerce} widths, geometries, or an existing table instead, see
\code{\link[=xt_as_channel]{xt_as_channel()}} (extra columns via \code{...} apply when building from
widths/\code{sfc}; add columns on the data frame first when coercing a table).
}
\details{
Every channel has a plan column; profile is optional. Set CRS on \code{.plan}
with \code{\link[sf:st_crs]{sf::st_crs()}} / \code{\link[sf:st_crs]{sf::st_set_crs()}} before calling if needed.

When \code{.profile} is supplied, \code{length(.profile)} must equal \code{length(.plan)}.
Additional columns are recycled with \code{\link[vctrs:vec_recycle]{vctrs::vec_recycle_common()}}.
}
\examples{
library(sf)
seg <- st_sfc(
  st_linestring(matrix(c(-1, 1, 0, 1), ncol = 2)),
  st_linestring(matrix(c(0, 0, 1, 1), ncol = 2)),
  crs = 3005
)
xt_channel(.plan = seg)
}
\seealso{
\code{\link[=xt_as_channel]{xt_as_channel()}} to coerce from widths, existing tables, etc.
}
