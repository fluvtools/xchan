% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xt_width.R
\name{xt_width}
\alias{xt_width}
\alias{xt_width.xchan}
\alias{xt_width.xsection}
\alias{xt_width.xs_profile}
\title{Width of cross sections}
\usage{
xt_width(x, ...)

\method{xt_width}{xchan}(x, ...)

\method{xt_width}{xsection}(x, ...)

\method{xt_width}{xs_profile}(x, ...)
}
\arguments{
\item{x}{An \code{\link{xchan}}, \code{\link{xsection}}, or \code{xs_profile} object.}

\item{...}{Unused (reserved for methods).}
}
\value{
For \code{\link{xchan}}: a numeric vector with one width per cross section, carrying
\code{\link[units:units]{units::units()}} when the channel has a CRS with a defined linear unit
(for example metres). When no CRS is set the result is plain numeric.
For \code{\link{xsection}}: a non-negative numeric scalar. If attribute \code{"crs"} is
set on \code{x} (unusual; the container \code{\link{xchan}} holds CRS instead), the result
may carry \code{\link[units:units]{units::units()}} like a channel with that CRS; otherwise plain
numeric.
For \code{xs_profile}: a non-negative numeric scalar (no CRS context, so plain
numeric).
}
\description{
\code{xt_width()} returns geometric width. For an \code{\link{xchan}}, this is one value per
cross section from planimetric line lengths. For an \code{\link{xsection}}, it is the
length of that plan polyline (the same value as the corresponding element of
\code{\link[=xt_width]{xt_width()}} on the parent channel). For a single \code{xs_profile} object, it is
the span along the profile horizontal axis between the outermost left and
right banks (the same convention as \code{\link[=xt_generate_profile]{xt_generate_profile()}} and
\code{\link[=xt_add_profile]{xt_add_profile()}}).
}
\examples{
library(sf)
seg <- st_sfc(
  st_linestring(matrix(c(-1, 0, 1, 0), ncol = 2, byrow = TRUE)),
  crs = 3005
)
coords <- matrix(c(-1, 0, 0, -1, 1, 0), ncol = 2, byrow = TRUE)
xs <- xchan:::new_profile(coords, bankpoints = c(-1, 1))
xt_width(xs)

# xt_width(demo_channel)
}
