% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xt_widen.R, R/xt_widen_by.R, R/xt_widen_times.R
\name{xt_widen}
\alias{xt_widen}
\alias{xt_widen_by}
\alias{xt_widen_times}
\title{Widen a river cross section}
\usage{
xt_widen(cross_section, ..., width, volume, side = "both")

xt_widen_by(object, by, prop_left = 0.5)

xt_widen_times(object, times, side = c("both", "left", "right"))
}
\arguments{
\item{cross_section}{An object of class \code{sx} or a subclass like \code{sx_1d} or \code{sx_2d}.}

\item{...}{Additional arguments. Not currently used, but required for S3 dispatch.}

\item{width}{The total width to add to the cross section. Must be a numeric value
and cannot be used with \code{volume}.}

\item{volume}{The total volume to remove to widen the cross section. Must be a
numeric value and cannot be used with \code{width}. Only applicable for 2D
cross sections.}

\item{side}{Which side of the cross section to widen by? One of
\code{"both"} (splits the change in width equally between both sides),
\code{"left"}, or \code{"right"} (applies the width change to one side of the
channel).}

\item{object}{A cross section object.}

\item{by}{Amount to widen the channel by, using units in common with the
cross sectional units. Negative values will narrow cross sections. Either
a vector of length equal to the number of cross sections, or length 1.}

\item{times}{Multiplicative factor to widen the channel by. Values less than
1 will narrow cross sections. Either
a vector of length equal to the number of cross sections, or length 1.}
}
\value{
A modified cross section object.
}
\description{
Widens a cross section by a specified width or volume,
distributing the change according to a given scheme.

Widen or narrow a collection of cross sections. \code{xt_widen_by()} widens
(narrows) by unit length; \code{xt_widen_times()} widens (narrows) by
a multiplicative factor.
}
\details{
Distributing the widening between the left and right banks through the
\code{side} argument is always done by running a \verb{distribute_p_*()} function;
for simplicity, a character string can be passed to \code{side} that replaces
\code{*} and uses the function defaults. Options are:
\itemize{
\item "left" or \code{distribute_p_left()}: specify how much of the widening
applies on the left bank.
\item "right" or \code{distribute_p_right()}: specify how much of the widening
applies on the right bank.
\item "both" or \code{distribute_p_both()}: specify how much of the widening
applies to both banks.
}
}
\note{
While the ellipsis \code{...} is currently not used, it forces the \code{width} and
\code{volume} arguments to be named to ensure deliberate specification.
}
\author{
Vincenzo Coia, Heba Abdelmoaty
}
