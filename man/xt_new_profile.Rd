% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xt_profile.R
\name{xt_new_profile}
\alias{xt_new_profile}
\title{Create profile cross section}
\usage{
xt_new_profile(coords, left_bank_distance, right_bank_distance)
}
\arguments{
\item{coords}{Matrix of coordinates with columns (distance, elevation)}

\item{left_bank_distance}{Distance of left bank from center}

\item{right_bank_distance}{Distance of right bank from center}
}
\value{
An xs_profile object with left and right sides
}
\description{
Create a profile cross section from coordinates and bank positions.
This function takes elevation data and bank positions and creates
an xs_profile object with left and right sides.
}
\details{
This function creates a profile cross section by:
\enumerate{
\item Injecting bank points at the specified distances
\item Finding thalwegs (lowest points) in the channel
\item Splitting the profile into left and right sides
\item Creating the xs_profile structure
}
}
\examples{
# Create a profile cross section
coords <- matrix(c(
  -5, 10,  # left side
  -2, 8,
  0, 5,   # center (thalweg)
  2, 8,
  5, 10   # right side
), ncol = 2, byrow = TRUE)

profile <- xt_profile(coords, left_bank_distance = -3, right_bank_distance = 3)
}
