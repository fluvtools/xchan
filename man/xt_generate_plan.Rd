% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xt_generate_plan.R
\name{xt_generate_plan}
\alias{xt_generate_plan}
\title{Generate channel object from banklines}
\usage{
xt_generate_plan(banks, ..., n, spacing, at, axis = NULL, progress = FALSE)
}
\arguments{
\item{banks}{Channel footprint as \strong{one closed polygon} (\code{POLYGON} or
\code{MULTIPOLYGON}), typically as \code{\link[sf:sf]{sf::st_sf()}}, \code{\link[sf:sfc]{sf::st_sfc()}}, or bare
\code{\link[sf:st_geometry]{sf::st_geometry()}}. The ring(s) enclose the plan-view channel area; left
and right banks are \strong{not} supplied as separate inputs—they are inferred
from this boundary together with the sampling axis (see \strong{Details}).

\strong{Not supported as \code{banks}:} two independent open bank polylines (e.g. one
\code{LINESTRING} per bank without closing the corridor). Automatic axis
generation uses \code{\link[centerline:cnt_path_guess]{centerline::cnt_path_guess()}}, which expects polygon
geometry; bank-to-bank segments are found by intersecting trial transects
with this closed boundary.}

\item{...}{Additional arguments (ignored).}

\item{n}{Number of cross sections to generate (mutually exclusive with
spacing and at)}

\item{spacing}{Distance between cross sections (mutually exclusive with n
and at)}

\item{at}{Specific distances along the channel axis for cross sections (mutually
exclusive with n and spacing)}

\item{axis}{Channel axis as a multilinestring: the line along which cross
sections are placed. If \code{NULL} (the default), an axis is generated
automatically using the \strong{centerline} package (\code{centerline::cnt_path_guess()}).}

\item{progress}{Logical; if \code{TRUE}, display a text progress bar while
generating planimetric cross sections.}
}
\value{
A channel object with planimetric cross sections in the plan column.
Rows follow downstream order along the sampling axis. After \code{arrange()} or
subsetting, restore order with \code{\link[=xt_arrange_downstream]{xt_arrange_downstream()}}. Use \code{\link[=xt_distance_ds]{xt_distance_ds()}}
for distance along the axis from its start to each section (requires the axis
from \code{\link[=xt_axis]{xt_axis()}}, which this function sets). The sampling axis is stored for
geometry that requires it.
}
\description{
Generate a channel object with planimetric cross sections from a
single closed polygon outlining the channel footprint (both banks).
}
\details{
\strong{Bank geometry:} Supply the channel as one polygon (or
multipolygon) so its boundary is a closed loop around the wetted/plan
corridor. If you only have two bank polylines, convert them to a closed
polygon (e.g. connect upstream/downstream ends) before calling this function.

This function takes the definition of "cross section" relative
to a point in the channel to be the line segment intersecting the point
whose bank-to-bank segment width is the smallest. Note that this does not
imply that the cross section is unique, and in this case the cross section
is arbitrarily taken to be the one closest to a 0-degree angle --
although in almost all cases this should not be an issue.

To define the spacing of the cross sections, a channel axis is
first calculated, and equally spaced points are sampled along that
axis. Cross sections are calculated at these points.

\strong{Downstream} is the direction of increasing distance along that axis
(the same direction used when stations are sorted by
\code{\link[sf:st_line_project_point]{sf::st_line_project()}}). If you supply \code{axis}, downstream follows the
storage order and digitization of that line; if the axis is generated
automatically, downstream follows the geometry returned by the centerline
routine.

\strong{Left and right bank} mean left and right when standing at the station on
the axis and \strong{facing downstream}, in the map plane of the CRS (planar
coordinates).

\strong{Vertex order (planimetric convention):} each output segment is a
\code{LINESTRING} from left bank to right bank: the \strong{first} coordinate is on the
left bank, the \strong{last} on the right bank. Increasing distance along the
segment (first to last vertex) thus matches profile cross sections where
distance increases from left to right bank.

\strong{How orientation is computed:} for each station we take a unit tangent to
the axis pointing downstream (\code{axis_unit_tangent_downstream()}). For each
endpoint of the bank-to-bank segment we form the vector from the station to
that endpoint and compute the 2D scalar cross product with the tangent,
\eqn{D_x (E_y - C_y) - D_y (E_x - C_x)}{D_x*(E_y-C_y) - D_y*(E_x-C_x)}
where \eqn{(D_x,D_y)} is the tangent and \eqn{(C_x,C_y)} / \eqn{(E_x,E_y)}
are the station and an endpoint. Under the usual planar orientation, the
endpoint with the \strong{larger} value lies on the \strong{left} bank. If that is not
already the first vertex, the segment is reversed with \code{\link[sf:geos_unary]{sf::st_reverse()}}
(\code{orient_plan_xs_left_first()}). This does not require splitting the bank
polygon by the axis.
}
\examples{
bl <- sf::st_sfc(demo_bankline, crs = 3005)
channel <- xt_generate_plan(bl, n = 100)

# With a custom axis (e.g. user-defined line along the channel)
# channel <- xt_generate_plan(bl, n = 100, axis = my_axis)
}
