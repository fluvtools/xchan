% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xt_profile_at.R
\name{xt_xsection_at}
\alias{xt_xsection_at}
\title{Get one cross section by downstream flow order}
\usage{
xt_xsection_at(channel, i, axis = NULL)
}
\arguments{
\item{channel}{A channel object (\code{\link{xchan}}).}

\item{i}{Single positive integer: position in downstream flow order (1 =
smallest chainage along \code{axis}).}

\item{axis}{Optional LINESTRING axis (\code{sfc} / \code{sfg}); if \code{NULL}, uses
\code{\link[=xt_axis]{xt_axis()}} on \code{channel}.}
}
\value{
\code{xt_xsection_at()}: one \code{\link{xsection}}.
}
\description{
\code{xt_xsection_at()} extracts a single \code{\link{xsection}} by \strong{flow order}: the
\code{i}th section when rows are sorted by increasing \code{\link[=xt_distance_downstream]{xt_distance_downstream()}}
along the channel axis (see \code{axis}). Internal helper \code{xt_profile_at()} returns
the embedded \code{xs_profile} for that section (requires profile geometry).
}
\examples{
coords <- matrix(c(-2, 10, 0, 8, 2, 10), ncol = 2, byrow = TRUE)
xs <- xchan:::new_profile(coords, bankpoints = c(-2, 2))
ch <- xchan:::set_channel_profile(xt_as_channel(c(4, 4), crs = 3005), list(xs, xs))
xt_xsection_at(ch, 1)
}
