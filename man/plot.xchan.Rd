% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.xchan.R
\name{plot.xchan}
\alias{plot.xchan}
\title{Plot a channel object}
\usage{
\method{plot}{xchan}(
  x,
  ...,
  extent = c("banks", "full"),
  axis = c("line", "arrows", "none"),
  banks = c("auto", "show", "hide"),
  add = FALSE
)
}
\arguments{
\item{x}{An \code{\link{xchan}} object.}

\item{...}{Additional arguments passed to \code{\link[=plot]{plot()}} for plan geometries and
to bank-marker styling (\code{col_bank}, \code{pch_bank}, \code{cex_bank}).}

\item{extent}{One of \code{"banks"} (default) or \code{"full"}.
\code{"full"} draws each transect out to the ends of its profile (when profiles
are present); uses bank-to-bank segments only if there is no profile
geometry (with a warning). Profile bank markers are drawn only when \code{extent = "full"} and bank markers are enabled (see \code{banks}).}

\item{axis}{How to draw the stored channel axis (\code{\link[=xt_axis]{xt_axis()}}), when one is
present: \code{"line"} (default) draws the axis as a plain line, \code{"arrows"}
draws flow direction along the axis, \code{"none"} omits it.

When \code{\link[=xt_bankline]{xt_bankline()}} is set and \code{add = FALSE}, the footprint is drawn first
(filled polygon under transects), then cross sections and the axis overlay.}

\item{banks}{One of \code{"auto"}, \code{"show"}, or \code{"hide"}: whether to draw bank
markers on planimetric transects (endpoints, and all profile banks when
\code{extent = "full"}). \code{"auto"} draws markers when there is no \code{\link[=xt_bankline]{xt_bankline()}}
footprint, and omits them when a footprint is present (markers are
redundant with the polygon boundary). \code{"show"} / \code{"hide"} override that
rule.}

\item{add}{If \code{TRUE}, draw on the current plot (same rules as
\code{\link[graphics:plot.default]{graphics::plot()}}).}
}
\description{
Plot a channel object
}
\details{
For the plan view, when \code{add = FALSE} and you do not pass \code{xlim} / \code{ylim},
the plot limits are adjusted so the on-screen aspect is not extremely
stretched (for example when synthetic width-only channels use a large default
station spacing relative to transect width). Pass your own \code{xlim} and \code{ylim}
to reproduce the raw map scale.

For "profile" and "3d" views, you can specify \code{exaggerate} to vertically
exaggerate the relief. For example, \code{exaggerate = 2} doubles the vertical
scale. The default is \code{exaggerate = 1} (no exaggeration). It is strongly
recommended not going beyond 3, because exaggeration beyond this point can
distort the perception of the profile.
}
