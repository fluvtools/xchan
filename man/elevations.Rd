% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elevations.R
\name{elevation_thalweg}
\alias{elevation_thalweg}
\alias{elevations}
\alias{elevation_bank}
\alias{elevation_bank_left}
\alias{elevation_bank_right}
\alias{elevation_bed}
\title{Elevation specifications for channel profiles}
\usage{
elevation_thalweg()

elevation_bank(.f = min, ...)

elevation_bank_left()

elevation_bank_right()

elevation_bed(.f = mean, ...)
}
\arguments{
\item{.f}{Numeric summary function applied per cross section. Each call uses
a single
numeric vector \code{x} as the first argument to \code{.f} (not separate left/right
arguments); further arguments are forwarded from \code{...}: *
\code{elevation_bank()} — \code{x} has length 2: outer \strong{left} then \strong{right} bank
elevations (\code{c(z_left, z_right)}). Default \link[base:Extremes]{base::min} returns the lower
bank elevation; \link[base:mean]{base::mean} averages the two banks. * \code{elevation_bed()} —
\code{x} is the vector of elevation (\code{z}) values at \strong{encoded} profile vertices
on the \strong{wetted bed} (within each water interval between banks; islands
excluded). Default \link[base:mean]{base::mean} is an unweighted mean of those values.}

\item{...}{Further arguments forwarded to \code{.f} (for example \code{probs} for
\link[stats:quantile]{stats::quantile}).}
}
\value{
An object inheriting \code{"xchan_elevation"}: a function \code{(channel)}
that returns
a numeric vector of elevations, one per cross section in storage order
(\verb{[[i]]} of \code{channel} when it is an \code{\link{xchan}}). Also used as \code{reference} in
other package functions expecting an elevation specification.
}
\description{
These functions construct \strong{elevation specification} objects (class
\code{"xchan_elevation"}). An elevation specification is a small callable wrapper:
passing it as \code{reference} to \code{\link[=xt_elevation]{xt_elevation()}} runs it against the channel and
returns one elevation per cross section (aligned with \verb{[[i]]} storage order
of the \code{\link{xchan}}).
}
\details{
\code{\link[=elevation_thalweg]{elevation_thalweg()}} takes no arguments: each section contributes its stored
\code{thalweg_elev} value — the minimum elevation among wetted profile vertices —
documented as a return component of each \code{xs_profile}.
}
\section{Functions}{
\itemize{
\item \code{elevation_bank()}: Banks: apply \code{.f(x, ...)} where \code{x} is \code{c(z_left, z_right)} at the
\strong{outer left} and \strong{outer right} bank vertices (same points as
\code{\link[=elevation_bank_left]{elevation_bank_left()}} / \code{\link[=elevation_bank_right]{elevation_bank_right()}}). Default \code{.f = min}
returns the lower bank elevation.

\item \code{elevation_bank_left()}: Elevation (\code{z}) at the \strong{outer left bank} profile
vertex only.

\item \code{elevation_bank_right()}: Elevation (\code{z}) at the \strong{outer right bank} profile
vertex only.

\item \code{elevation_bed()}: Apply \code{.f(x, ...)} to encoded vertex elevations \code{x} on
the
\strong{wetted bed} (within each water interval between banks; dry islands
excluded). Vertices are included or excluded by distance along the profile
only; \code{.f} does not interpolate or integrate along the bed. Default \code{.f = mean} is an unweighted mean of those \code{z} values.

}}
\section{Profile geometry and naming}{


Objects of class \code{xs_profile} hold a matrix \code{coordinates} with column 1 =
\strong{distance along the cross section} (left bank toward right bank,
increasing) and column 2 = \strong{elevation}. \strong{Bank} points are vertices on
that polyline flagged in \code{banks} (row indices); \strong{thalweg} identifies the
deepest part of the traced section.

In this vertical slice, \strong{left} and \strong{right} mean smaller vs larger
distance along the profile (typically left and right banks in map view when
the section is oriented consistently). \code{elevation_bed()} summarizes
elevations at \strong{stored profile vertices} whose distance lies on the \strong{wetted
bed}: inside each \strong{water} interval between consecutive banks (alternating
water / land / water along the section; see \code{\link[=xt_add_profile]{xt_add_profile()}} and
\code{xs_profile} structure). Dry islands and floodplain tails outside the outer
banks are excluded. The summary is applied to those vertex elevations only —
there is no interpolation along the bed, no distance weighting, and no
integration over width; default \link[base:mean]{base::mean} is an unweighted arithmetic mean
of the encoded \code{z} values (sampling density along the profile therefore
affects it).
}

\seealso{
\code{\link[=xt_elevation]{xt_elevation()}}, \code{\link[=xt_gradient]{xt_gradient()}}, \code{\link[=xt_add_profile]{xt_add_profile()}}
}
