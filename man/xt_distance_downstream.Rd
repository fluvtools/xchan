% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xt_distance_downstream.R
\name{xt_distance_downstream}
\alias{xt_distance_downstream}
\alias{xt_distance_upstream}
\title{Distance along the channel axis to cross-section midpoints}
\usage{
xt_distance_downstream(channel, axis = NULL)

xt_distance_upstream(channel, axis = NULL)
}
\arguments{
\item{channel}{An \code{\link{xchan}} with planimetric cross sections.}

\item{axis}{Optional \strong{LINESTRING} (\code{sfc} / \code{sfg}). Resolution matches
\code{\link[=xt_trace_centerline]{xt_trace_centerline()}}: use this geometry, else \code{xt_axis(channel)}, else an
error (set an axis with \code{xt_axis(channel) <- ...} or use \code{\link[=xt_generate_plan]{xt_generate_plan()}}).}
}
\value{
A numeric vector of length \code{length(channel)} (same section order as \code{channel}). The
result carries \code{\link[units:units]{units::units()}} when the channel has a CRS with a defined linear unit;
plain numeric otherwise.
}
\description{
\strong{\code{xt_distance_downstream()}} returns distance measured along the axis \strong{from its start}
to each cross section's bank-to-bank midpoint. \strong{\code{xt_distance_upstream()}} returns
distance along the axis \strong{from each midpoint to the end} of the axis (equivalently:
axis length minus downstream distance). Together they satisfy
\code{xt_distance_downstream(x) + xt_distance_upstream(x) == axis_length} at each section when
lengths are numeric.
}
\note{
Use \code{\link[=xt_arrange_downstream]{xt_arrange_downstream()}} if you need sections ordered by downstream chainage.
}
\examples{
\donttest{
ch <- xt_generate_plan(fraser_bankline, n = 5)
xt_distance_downstream(ch)
xt_distance_upstream(ch)
}

}
