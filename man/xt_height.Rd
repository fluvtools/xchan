% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xt_height.R
\name{xt_height}
\alias{xt_height}
\title{Channel height using elevation specifications}
\usage{
xt_height(channel, lower = elevation_thalweg(), upper = elevation_bank())
}
\arguments{
\item{channel}{A channel object with profile cross sections}

\item{lower}{Lower elevation specification (defaults to thalweg)}

\item{upper}{Upper elevation specification (defaults to minimum bank elevation)}
}
\value{
A vector of heights, one for each cross section
}
\description{
Calculate channel height as the difference between upper and lower elevation references.
}
\details{
This function calculates channel height as the difference between
upper and lower elevation references. The elevation specifications determine
which elevation values are used for the calculation.
}
\examples{
# Get height from thalweg to minimum bank elevation
heights <- xt_height(channel)

# Get height from thalweg to water surface
heights <- xt_height(channel, upper = elevation_column("water_surface"))

# Get height from channel bottom to maximum bank elevation
heights <- xt_height(channel, 
                    lower = elevation_bottom(.f = mean),
                    upper = elevation_bank(.f = max))
}
