% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xt_section_id.R
\name{xt_section_id}
\alias{xt_section_id}
\alias{xt_section_id.xchan}
\alias{xt_section_id.default}
\alias{xt_section_id<-}
\alias{xt_section_id<-.xchan}
\alias{xt_section_id<-.default}
\title{Cross-section identity keys on a channel}
\usage{
xt_section_id(channel)

\method{xt_section_id}{xchan}(channel)

\method{xt_section_id}{default}(channel)

xt_section_id(channel) <- value

\method{xt_section_id}{xchan}(channel) <- value

\method{xt_section_id}{default}(channel) <- value
}
\arguments{
\item{channel}{An \code{\link{xchan}} object.}

\item{value}{\code{NULL} removes stored keys (downstream functions then treat section
indices as \code{seq_len(length(channel))}). Otherwise an atomic vector of length
\code{length(channel)} with \strong{no} \code{NA}s and \strong{no} duplicates.}
}
\value{
For \code{xt_section_id()}, the stored vector or \code{NULL}. For assignment, an
updated \code{\link{xchan}}.
}
\description{
Get or set per-section \strong{stable keys} stored on an \code{\link{xchan}}. These keys match
the \code{section} column when attaching tabular profiles with \code{\link[=xt_add_profile]{xt_add_profile()}}.
}
\details{
Keys are stored as attribute \code{"section_i"} on the \code{\link{xchan}} (the name is
historical). \code{\link[=xchan]{xchan()}} initializes them to \code{seq_len(n)}.
}
\examples{
ch <- xchan(list(
  xsection(matrix(c(0, 0, 1, 0), ncol = 2, byrow = TRUE)),
  xsection(matrix(c(0, 1, 1, 1), ncol = 2, byrow = TRUE))
))
xt_section_id(ch)
xt_section_id(ch) <- c("A12", "B04")
xt_section_id(ch)
xt_section_id(ch) <- NULL
xt_section_id(ch)
}
\seealso{
\code{\link[=xt_add_profile]{xt_add_profile()}}, \code{\link[=xchan]{xchan()}}
}
