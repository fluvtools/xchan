% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.xs_profile.R
\name{plot.xs_profile}
\alias{plot.xs_profile}
\title{Plot profile cross section}
\usage{
\method{plot}{xs_profile}(
  x,
  ...,
  extent = c("banks", "full"),
  add = FALSE,
  exaggerate = 1,
  from = NULL,
  to = NULL,
  legend = TRUE
)
}
\arguments{
\item{x}{An xs_profile object}

\item{...}{Additional arguments passed to plot}

\item{extent}{Selects the default horizontal span: \code{"banks"} uses the span
between the outer bank distances; \code{"full"} uses the full range of profile
sample distances (default \code{"banks"}).}

\item{add}{Logical. Add to existing plot?}

\item{exaggerate}{Positive numeric. \strong{Physical} vertical exaggeration:
passed
as \code{asp} to \link[graphics:plot.default]{graphics::plot.default}, so one data unit along \strong{y} is drawn
with the same length as \code{exaggerate} data units along \strong{x} (default \code{1}
gives true 1:1 scaling on the device). Values above 1 stretch the profile
vertically; values between 0 and 1 compress it. Ignored when \code{add = TRUE}.}

\item{from, to}{Optional adjustments to that window (distance along the
profile). The plotted range starts at \code{from} when given, otherwise at the
default left edge for \code{extent}; it ends at \code{to} when given, otherwise at
the default right edge. You may pass \code{from} only, \code{to} only, both, or
neither. Values may lie outside the data range (empty band on that side).}

\item{legend}{Logical. Draw the legend identifying the profile line, bank
points, and thalweg points? Defaults to \code{TRUE}. Set \code{FALSE} to omit it
(e.g. for figures where the elements are self-explanatory). Ignored when
\code{add = TRUE}.}
}
\value{
A plot of the profile cross section
}
\description{
Plot a profile cross section showing the elevation profile. This function is
primarily for internal use or advanced users. Most users should work with
channel objects using plot_plan() or plot_3d().
}
\details{
Elevation values are not rescaled; only the plot aspect ratio changes. For
actually modifying stored elevations, use \code{\link[=xt_exaggerate_relief]{xt_exaggerate_relief()}}.

Finite \code{asp} (from \code{exaggerate}) is implemented by \link[graphics:plot.window]{graphics::plot.window},
which \strong{may expand} \code{xlim} / \code{ylim} so the aspect ratio can be satisfied in
the physical plotting region. That is strongest for very large \code{exaggerate};
see \code{?plot.window} (\code{asp}). It is not mis-handling of \code{from} /
\code{to} in this package.

For the horizontal axis, \code{extent} fixes the reference span (\code{rng_default}).
Each of \code{from} and \code{to} overrides one endpoint only when supplied; omitted
endpoints use \code{rng_default}.
}
\examples{
# Plot a profile cross section (advanced use)
channel <- xt_as_channel(rep(1, 6))
channel <- xt_add_profile(
  channel,
  distance = distance,
  elevation = elevation,
  section = id,
  banks = is_bank,
  data = profile_survey
)
profile_object <- channel[[1]]$profile
plot(profile_object)

# Plot with vertical exaggeration
plot(profile_object, exaggerate = 2)

# Narrow or shift the window relative to `extent`
plot(profile_object, extent = "full", from = -100)
plot(profile_object, extent = "banks", to = 50)
}
