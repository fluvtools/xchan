% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xt_extend_frame_left.R,
%   R/xt_extend_frame_right.R
\name{xt_extend_frame_left}
\alias{xt_extend_frame_left}
\alias{xt_extend_frame_right}
\title{Extend topographic frame on one bank}
\usage{
xt_extend_frame_left(channel, extender)

xt_extend_frame_right(channel, extender)
}
\arguments{
\item{channel}{Channel object with profile cross sections.}

\item{extender}{Extender operator from the \verb{extender_*()} family (e.g.
\code{\link[=extender_flat]{extender_flat()}}, \code{\link[=extender_slope]{extender_slope()}}).}
}
\value{
A modified channel object whose profile frame is extended on the
requested side (once implemented; currently returns \code{channel}
unchanged).
}
\description{
\code{xt_extend_frame_left()} extends the profile cross-section beyond the
\strong{left} bank only; \code{xt_extend_frame_right()} extends beyond the \strong{right}
bank only. Each adds topography using \code{\link[=extender_flat]{extender_flat()}} /
\code{\link[=extender_slope]{extender_slope()}} (and related) operators so there is space for widening
or erosion calculations on that side.

For symmetric extension on \strong{both} banks in one step, see
\code{\link[=xt_extend_frame]{xt_extend_frame()}}.
}
\details{
Extenders are operator factories: they build functions that define how far
and how to extend topography beyond the bank. Side-specific functions apply
that extension only on the left or right half of each profile.
}
\examples{
# Flat extension on the left bank only
channel <- xt_as_channel(c(10, 12, 8, 15, 11, 9))
channel <- xt_extend_frame_left(
  channel,
  extender = extender_flat(extent = 20)
)

# Sloped extension on the right bank only
channel <- xt_extend_frame_right(
  channel,
  extender = extender_slope(extent = 30, slope = 0.02)
)

}
\seealso{
\code{\link[=xt_extend_frame]{xt_extend_frame()}} to extend both banks together.
}
