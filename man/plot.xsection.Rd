% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.xsection.R
\name{plot.xsection}
\alias{plot.xsection}
\title{Plot one cross section (\code{xsection})}
\usage{
\method{plot}{xsection}(
  x,
  ...,
  view = c("auto", "profile", "plan"),
  extent = c("banks", "full"),
  add = FALSE,
  exaggerate = 1,
  from = NULL,
  to = NULL,
  col = "black",
  lwd = 1,
  banks = c("auto", "show", "hide"),
  col_bank = "deepskyblue3",
  pch_bank = 16,
  cex_bank = 0.65,
  warn_if_no_profile = TRUE
)
}
\arguments{
\item{x}{An \code{\link{xsection}} object.}

\item{...}{Additional arguments passed to \code{\link{plot.xs_profile}}
(profile view) or forwarded when plotting the plan view.}

\item{view}{\code{"auto"} plots the profile when \code{x$profile} is present, otherwise
the plan view. Use \code{"profile"} or \code{"plan"} to force one view.}

\item{extent}{Selects the default horizontal span: \code{"banks"} uses the span
between the outer bank distances; \code{"full"} uses the full range of profile
sample distances (default \code{"banks"}).}

\item{add}{Logical. Add to existing plot?}

\item{exaggerate}{Positive numeric. \strong{Physical} vertical exaggeration: passed
as \code{asp} to \link[graphics:plot.default]{graphics::plot.default}, so one data unit along \strong{y} is drawn
with the same length as \code{exaggerate} data units along \strong{x} (default \code{1}
gives true 1:1 scaling on the device). Values above 1 stretch the profile
vertically; values between 0 and 1 compress it. Ignored when \code{add = TRUE}.}

\item{from, to}{Optional adjustments to that window (distance along the
profile). The plotted range starts at \code{from} when given, otherwise at the
default left edge for \code{extent}; it ends at \code{to} when given, otherwise at
the default right edge. You may pass \code{from} only, \code{to} only, both, or
neither. Values may lie outside the data range (empty band on that side).}

\item{col, lwd, banks, col_bank, pch_bank, cex_bank, warn_if_no_profile}{Used for the plan view only (see \code{\link{plot.xchan}}).}
}
\value{
Called for its graphical side effect.
}
\description{
Plot the profile view when a profile exists (default), or the planimetric
transect. For profile-only plots you can also call \code{plot(x$profile)} on the
embedded \code{xs_profile}.
}
\examples{
channel <- xt_as_channel(rep(1, 6))
channel <- xt_add_profile(
  channel,
  distance = distance,
  elevation = elevation,
  section = id,
  banks = is_bank,
  data = profile_survey
)
plot(channel[[1]])

}
