% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xt_trace_thalwegline.R
\name{xt_trace_thalwegline}
\alias{xt_trace_thalwegline}
\title{Trace a line through thalweg positions in plan view}
\usage{
xt_trace_thalwegline(channel, axis = NULL)
}
\arguments{
\item{channel}{Channel object with \strong{both} planimetric cross sections and a
profile view (\code{xs_profile} list).}

\item{axis}{Optional LINESTRING axis used to define downstream section order.
If \code{NULL}, uses \code{xt_axis(channel)}; if that is also \code{NULL}, an error is
raised (same ordering rules as \code{\link[=xt_trace_centerline]{xt_trace_centerline()}}).}
}
\value{
An \code{sfc} object:
\itemize{
\item \strong{Single thalweg} at every section: one \code{LINESTRING}.
\item \strong{Several thalwegs} (e.g. after widening): one \code{MULTILINESTRING} whose
parts are ordered \strong{left to right} along each cross section (by distance
along the section). Part \code{j} links the \code{j}-th thalweg across downstream
stations (requires the \strong{same} number of thalwegs at every section).
}
}
\description{
Projects each profile’s thalweg distance onto the corresponding planimetric
cross section (bank-to-bank segment), then connects those \verb{(x, y)} positions in
downstream order along the channel axis — analogous to \code{\link[=xt_trace_centerline]{xt_trace_centerline()}},
but through lowest-point (\code{thalweg}) locations rather than mid-channel.
}
\details{
Requires \code{\link[=xt_has_profile]{xt_has_profile()}}. Thalweg \verb{(distance, elevation)} pairs are taken from
the profile coordinate matrix at each \code{thalwegs} row index; positions along the
plan line use the same chord interpolation as \code{\link[=xt_as_sfc]{xt_as_sfc()}}
3D export with \code{extent = "banks"} (first plan vertex = left bank, last =
right bank).

The axis defines section order only. Thalweg geometry is always recomputed
from current plan/profile geometry, so this function naturally supports
retracing after geometry-changing operations.
}
\examples{
\donttest{
# Requires profiles — for example from xt_generate_profile()
# th <- xt_trace_thalwegline(channel_with_profiles)
# plot(th)
}
}
\seealso{
\code{\link[=xt_trace_centerline]{xt_trace_centerline()}}, \code{\link[=xt_axis]{xt_axis()}}
}
