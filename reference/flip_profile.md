# Flip a profile cross section

Flips a profile cross section so that the left side becomes the right,
and the right becomes the left.

## Usage

``` r
flip_profile(profile)
```

## Arguments

- profile:

  Single profile cross section

## Value

The original profile cross section with the left and right sides
switched. The distance values (along the cross section) are flipped in
sign.
